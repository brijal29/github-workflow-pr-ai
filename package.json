{
  "name": "portfolio-react-node",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm -w server run dev\" \"npm -w client run dev\"",
    "build": "npm -w server run build && npm -w client run build",
    "lint": "npm -w server run lint && npm -w client run lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1",
    "node-fetch": "^3.3.2"
  }
}
